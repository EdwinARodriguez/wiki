<%= form_for(admin_article_type) do |f| %>
  <% if admin_article_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_article_type.errors.count, "error") %> prohibited this admin_article_type from being saved:</h2>

      <ul>
      <% admin_article_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
